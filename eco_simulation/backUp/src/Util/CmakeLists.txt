# 从指定目录收集所有的源文件
file(GLOB Util_SOURCES "${PROJECT_SOURCE_DIR}/src/Util/*.cpp")

# 创建一个库
add_library(Util ${Util_SOURCES})

# 使项目的 include 目录可用

target_include_directories(Util PUBLIC ${PROJECT_SOURCE_DIR}/include/Util)