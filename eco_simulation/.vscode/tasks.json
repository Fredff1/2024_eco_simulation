{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake Build",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/build" // 指定构建目录
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
